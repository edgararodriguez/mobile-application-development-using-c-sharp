<?xml version="1.0" encoding="utf-8" ?>
<Shell
    x:Class="C971.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:termsPages="clr-namespace:C971.Pages.Terms">

    <!-- Simple single-tab app that shows Terms by default -->
    <TabBar>
        <ShellContent
            Title="Terms"
            Route="terms"
            ContentTemplate="{DataTemplate termsPages:TermsListPage}" />
    </TabBar>

</Shell>
